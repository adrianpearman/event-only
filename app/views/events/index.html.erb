events#index page

<h1>Events</h1>

<% @events.each do |current_event| %>
  <div class="events">
    <h3><%= current_event.name %></h3>
    <h4>Event: <%= current_event.city%></h4>
    <%= current_event.date %><br />
    <%= current_event.capacity %><br />
  </div>

  <% end %>
